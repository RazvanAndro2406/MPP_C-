<Window x:Class="ChatClient.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MPP Chat" Height="350" Width="480"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> <RowDefinition Height="120"/>  <RowDefinition Height="Auto"/> </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Messages from your friends:" FontWeight="Bold" Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,5"/>
        <TextBox x:Name="RcvMsgTxt" Grid.Row="1" Grid.ColumnSpan="2" 
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                 TextWrapping="Wrap" Background="#EBFFEA" Margin="0,0,0,10"/>

        <TextBlock Text="Select friend:" FontWeight="Bold" Grid.Row="2" Margin="0,0,0,5"/>
        <DataGrid x:Name="FriendsTable" Grid.Row="3" Margin="0,0,10,0"
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Friends' IDs" Binding="{Binding Id}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Text="Message:" FontWeight="Bold" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="3" Grid.Column="1">
            <TextBox x:Name="MsgTxt" Height="50" AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" Tag="Your message here"/>
            <Button x:Name="SendBtn" Content="Send message" FontWeight="Bold" 
                    Height="30" Margin="0,10,0,0" Click="HandleSendMessage"/>
        </StackPanel>

        <Button x:Name="LogoutBtn" Content="Logout" Grid.Row="4" 
                Width="75" HorizontalAlignment="Left" Margin="0,10,0,0"
                FontWeight="Bold" Click="HandleLogout"/>
    </Grid>
</Window>